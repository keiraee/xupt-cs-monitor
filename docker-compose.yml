services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3080:80"
    depends_on:
      - backend
    restart: unless-stopped
